# 盘口波动套利 V2.0

## 身份

- 内部 `strategy_id`: `fast_directional_hedge_simple`（保留以兼容现有状态、启动脚本和电报选择）
- 展示名称：`盘口波动套利`
- 版本：`2.0`
- 默认不启用，不改变当前实盘默认策略

## 交易逻辑

1. 只用新鲜的 Polymarket UP/DOWN 可执行盘口选择领先侧，不等待 BTC 或 Price to Beat 同向确认。
2. 领先侧 ask 位于 `0.53～0.60`、ask 差至少 `0.04`、spread 不超过 `0.04`，并连续两个不同盘口 Tick 成立后入场。
3. 固定买入 `2` 份，FAK成交；入场最多追价 `0.02`，允许部分成交。
4. 持仓后按实际数量计算可卖 Bid VWAP。预计净利润为：

   `卖出价 - 买入价 - 预计买入手续费 - 预计卖出手续费`

5. 每份预计净利润达到 `0.02 pUSD` 时，立即用 FAK 卖出原方向仓位，记录为 `VOLATILITY_ARBITRAGE_PROFIT`。
6. 一个窗口最多完成两次独立入场；套利退出后可以寻找下一次新盘口机会。

## 风险控制

- BTC数据不参与方向确认和普通止损确认，只保留程序级数据陈旧、断线和异常风控。
- 未达到套利止盈时继续使用可执行 Bid 的初始止损、移动止损和 NORMAL/FAST/EMERGENCY 三级触发。
- 止损后比较卖出原仓与买入反向Token的净回收，选择更有利的风险退出路径。
- 部分成交、提交状态不确定、重启恢复和剩余裸露仓位处理沿用原有安全机制。

## 默认参数

- 入场：`0.53～0.60`
- 连续确认：`2 Tick`，不同Tick最短间隔 `150ms`
- 基础数量：`2份`
- 每份净止盈：`0.02 pUSD`（已经扣除预计双边Taker手续费）
- 止盈确认：`1 Tick`
- 每窗口最多入场：`2次`
- 正常入场时间：剩余 `180～60秒`
- 剩余 `30秒` 后禁止新开仓；剩余 `15秒` 后仅允许风险处理
